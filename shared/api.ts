/**
 * Shared code and Useful to share types between client and server
 * and small pure JS functions that can be used on both client and server
 */

export interface DemoResponse {
  message: string;
}
